<% if @student.active %>
  <% @student.active = false %>
  <%= "The student is currently inactive" %>
<% else %>
  <% @student.active = true %>
  <%= "The student is currently active" %>
<% end %>
<% @student.active %>